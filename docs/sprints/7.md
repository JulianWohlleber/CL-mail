# Sprint 7 — Release-prep + full team code review

**Lead:** Maya (eng lead), Tom (polish) — with the whole team reviewing
**Tag:** v0.1.7

## Research / Review

The whole team did a pass on the V0.1.6 codebase, each on their area:

- **Maya** — architecture, IPC boundaries, error handling, the services registry
- **David** — IMAP sync, SQLite migrations, CalDAV client, draft IPC
- **Priya** — React render correctness, Zustand selectors, hook deps, leaks
- **Tom** — dead code, paper cuts, stray console noise, TODO/FIXME
- **Sam** — edge cases, regression-coverage gaps, error paths
- **Lena** — IA consistency, button copy, empty states, flow clarity
- **Felix** — spacing, color-token usage, visual consistency, focus order

Findings were collected, then each one adversarially verified to drop false
positives before landing fixes. See `docs/sprints/7-review.md` for the full
report.

## Design / Development

Confirmed-real findings fixed this sprint, plus release-prep:

- An **About / version** surface so users can see what build they're on
- **Local-only, opt-in usage counters** (never leaves the machine) so the team
  can reason about which features matter — strictly local, no network
- Fixes for the confirmed review findings

## Testing

- Every confirmed finding that represented a regression risk got a pinning test
- Full regression suite green before tag
